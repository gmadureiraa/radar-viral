import { ComingSoon } from "@/components/coming-soon";

export default function SettingsPage() {
  return (
    <ComingSoon
      title="Configurações"
      description="Nichos ativos, fontes trackadas (IG handles, YouTube channels, newsletters), conexão Gmail."
      v1Path="/#settings"
    />
  );
}
