import { ComingSoon } from "@/components/coming-soon";

export default function AdminPage() {
  return (
    <ComingSoon
      title="Admin"
      description="KPIs globais, custos por geração, gerenciar usuários e fontes. Migrar quando UI v2 estiver completa."
      v1Path="/#admin"
    />
  );
}
